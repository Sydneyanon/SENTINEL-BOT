# nixpacks.toml - Force Python detection and pip install on Railway

providers = ["python"]

[phases.setup]
nixPkgs = ['python3']

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python main.py"
