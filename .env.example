# ============================================================
# TELEGRAM BOTS
# ============================================================

# Telegram Channel Bot (for posting signals)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHANNEL_ID=@your_channel_username

# Admin Bot (for checking stats via Telegram)
# Get token from @BotFather, get your user ID from @userinfobot
ADMIN_BOT_TOKEN=your_admin_bot_token_here
ADMIN_USER_IDS=123456789

# ============================================================
# DATA SOURCES
# ============================================================

# DexScreener API (for token data)
DEXSCREENER_API=https://api.dexscreener.com/latest/dex

# Whistle.ninja API (pump.fun mirror - no auth needed)
WHISTLE_API=https://pump.whistle.ninja

# KOLscan API (for influencer tracking)
# Get API key from https://kolscan.io/api
KOLSCAN_API_URL=https://api.kolscan.io
KOLSCAN_API_KEY=your_kolscan_api_key_here
MIN_KOL_TIER=verified  # Options: 'top', 'verified', 'all'

# ============================================================
# POLLING INTERVALS (seconds)
# ============================================================

# Main monitors
POLL_INTERVAL=45                       # DexScreener polling (if used)
GRADUATING_POLL_INTERVAL=90            # Check graduating tokens (85%+ curve)
KOLSCAN_POLL_INTERVAL=120              # Check KOL mentions

# Performance tracking
PERFORMANCE_CHECK_INTERVAL_SEC=1800    # 30 minutes
MOMENTUM_CHECK_INTERVAL_SEC=900        # 15 minutes
OUTCOME_CHECK_INTERVAL_SEC=3600        # 1 hour

# ============================================================
# SIGNAL FILTERS & THRESHOLDS
# ============================================================

# Conviction scoring
MIN_CONVICTION_SCORE=80                # Minimum score to post signal (0-100)

# Liquidity/Volume filters
MIN_LIQUIDITY_USD=5000                 # Minimum liquidity
MIN_VOLUME_24H=10000                   # Minimum 24h volume

# Graduating monitor settings
MIN_BONDING_CURVE=85.0                 # Min % bonding curve (85-95 for early entry)

# Win/Loss definitions
WIN_THRESHOLD=100                      # +100% = win (2x)
LOSS_THRESHOLD=-50                     # -50% = loss
EVALUATION_WINDOW_HOURS=24             # Hours to evaluate win/loss

# ===== EARLY DETECTION SYSTEM =====

# Telegram Monitoring (get from https://my.telegram.org)
TG_API_ID=your_telegram_api_id
TG_API_HASH=your_telegram_api_hash
TG_SESSION_NAME=sentinel_tg_session

# Telegram Channels to Monitor (comma-separated)
# Example: username1,username2,username3
TG_MONITORED_CHANNELS=solana_calls_public,pump_fun_gems,memecoin_alpha

# Twitter/X Monitoring
# Note: twscrape requires adding accounts via CLI
# Run: twscrape add_accounts accounts.txt username:password:email:email_password
# Then: twscrape login_accounts

# Early Detection Settings
MIN_EARLY_CONVICTION=85     # Minimum score to post early signal (default: 85)
SIGNAL_AGGREGATION_WINDOW=300  # Seconds to aggregate signals (default: 300)

# Helius webhook for smart money (you'll need to configure this in Helius dashboard)
# Add all wallet addresses from curated_wallets.py to an "Enhanced Transactions" webhook
# Webhook URL: https://your-app.up.railway.app/webhook/smart-money

# ============================================================
# OPTIONAL FEATURES
# ============================================================

# Database path
DB_PATH=sentinel.db

# Port for health checks (Railway)
PORT=8080

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
```

---

## 🎯 **Key Changes:**

### **Added:**
✅ `WHISTLE_API` - For pump.fun data  
✅ `KOLSCAN_API_URL` + `KOLSCAN_API_KEY` - KOL tracking  
✅ `MIN_KOL_TIER` - Filter KOL quality  
✅ `GRADUATING_POLL_INTERVAL` - Early entry signals  
✅ `KOLSCAN_POLL_INTERVAL` - KOL check frequency  
✅ `MIN_LIQUIDITY_USD` - Liquidity filter  
✅ `MIN_VOLUME_24H` - Volume filter  
✅ `MIN_BONDING_CURVE` - Graduating threshold  
✅ Better organization with sections

### **Kept:**
✅ All your existing variables  
✅ Your current thresholds  
✅ Your admin bot setup

---

## 📋 **Railway Variables to Set:**

When deploying, you need to set these in Railway:

**Required:**
```
TELEGRAM_BOT_TOKEN=<your_signal_bot_token>
TELEGRAM_CHANNEL_ID=@your_channel
ADMIN_BOT_TOKEN=<your_admin_bot_token>
ADMIN_USER_IDS=<your_telegram_id>
```

**Highly Recommended:**
```
KOLSCAN_API_KEY=<get_from_kolscan.io>
MIN_CONVICTION_SCORE=70
```

**Optional (defaults work):**
```
MIN_BONDING_CURVE=85.0
GRADUATING_POLL_INTERVAL=90
KOLSCAN_POLL_INTERVAL=120
MIN_KOL_TIER=verified
